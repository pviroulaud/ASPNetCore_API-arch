apiVersion: v1
kind: Service
metadata:
  name: certificateapi-np
spec:
  type: NodePort
  selector:
    app: certificateapi
  ports:
    - name: certificateapi
      protocol: TCP
      port: 80
      targetPort: 80
#kubectl apply -f certificateapi-nodeport.yaml      